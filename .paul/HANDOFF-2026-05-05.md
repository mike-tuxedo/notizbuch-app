# PAUL Handoff

**Date:** 2026-05-05
**Status:** paused (loop closed cleanly between plans)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Notizbuch — Offline-first PWA für handschriftliche Notizen mit E2E-verschlüsselter P2P-Sync.
**Core value:** Privatsphäre für handschriftliche Notizen — kein Server vertraut Inhalte.

---

## Current State

**Milestone:** v0.1 Daily-Driver Readiness (in progress)
**Phase:** 1 of 2 — UI-Optimierung
**Plans:** 2 closed (01-01, 01-02), 1 queued (01-03)
**Loop:** Idle (zwischen Plans)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     (für 01-02 — bereit für nächsten PLAN)
```

---

## What Was Done This Session

### Plan 01-01 — Mobile Touch-Targets, Safe-Areas, Topbar-Restruktur, Kontrast (geschlossen)
- `viewport-fit=cover` + `env(safe-area-inset-*)` an Sidebar/Toolbar/Pagebar/Footer
- Mobile-Topbar einzeilig (Portrait-Doppelreihe entfernt)
- `#btn-mobile-color` und `#btn-mobile-size` schweben absolut rechts oben (im Mobile, NICHT in Zen-Mode)
- Touch-Targets: alle ≥ 44×44 außer `tool-btn` (40×44 — pragmatischer Kompromiss wegen 8 Toolbar-Buttons auf 360px-Viewports)
- Kontrast-Lift: `--text-dim` #7a7692 → #b0acc8 (~8.5:1), `--text-faint` #3e3b56 → #6c6886 (~3.8:1)
- Files: `app.html`

### Plan 01-02 — PWA Install Fix (geschlossen)
- Neue Icons `img/icon-192.png` + `img/icon-512.png` (Lanczos-Upscale aus 180×180)
- `manifest.json`: 2 Icon-Einträge mit `purpose: "any maskable"`
- `sw.js`: `CACHE_NAME` v42 → v43, neue Pfade in `OPTIONAL_ASSETS`
- User-bestätigt: PWA installiert sich jetzt im Standalone-Modus auf Android

---

## What's Queued: Plan 01-03 — Zen-Mode-Layout-Fix

**Problem (vom User beim Re-Test gefunden):**
1. Zen-Mode Toolbar steht nicht "ganz oben" — erbt `padding-top: var(--safe-top)` und schiebt Icons unter Notch-Zone
2. Großer Spalt zwischen Tool-Group und Color/Size-Group → leftmost Icon (Pen) kann in Selfie-Cam-Zone landen
3. User-Selfie-Cam ist links oben (variiert je Gerät)

**Ursachenanalyse:**
- `.toolbar-right { margin-left: auto }` aus 01-01 (Mobile-MQ) überschreibt zen-modes `justify-content: center`
- Zen-mode Toolbar erbt `padding-top: var(--safe-top)` aus base `.toolbar`-Regel

**Geplante Fixes (3 kleine CSS-Overrides in mobile-MQ unter `body.zen-mode`):**
- `.toolbar { padding-top: 0 }` — Toolbar geht ganz nach oben
- `.toolbar-right { margin-left: 0 }` — Center-Justify aus zen-mode greift wieder
- Gap zwischen den Gruppen reduzieren falls noch nötig

**Boundary-Check:** `app.html` — diese Datei wird wieder modifiziert (war in 01-02 DO NOT CHANGE, weil 01-02 Manifest-Plan).

---

## Open Deferred Issues (in STATE.md)

| Issue | Effort |
|-------|--------|
| Zen-Mode-Layout-Fix (Plan 01-03 queued) | S |
| Mobile-Toolbar konsolidieren — `tool-btn` Width 40→44px durch Reduktion sichtbarer Buttons | M |
| 512er-Icon ist Upscale — Hi-Res-Master-Replace bei Bedarf | S |

---

## Files Touched This Session

| File | Change |
|------|--------|
| `app.html` | Modified (Plan 01-01 — gesamte UI-Optimierung) |
| `manifest.json` | Modified (Plan 01-02 — 2 neue Icon-Einträge) |
| `sw.js` | Modified (Plan 01-02 — CACHE_NAME, OPTIONAL_ASSETS) |
| `img/icon-192.png` | Created (Plan 01-02) |
| `img/icon-512.png` | Created (Plan 01-02) |
| `.paul/PROJECT.md` | Created (init) |
| `.paul/ROADMAP.md` | Created/updated (init + plan progress) |
| `.paul/STATE.md` | Created/updated (laufend) |
| `.paul/paul.json` | Created/updated (laufend) |
| `.paul/config.md` | Created (init — SonarQube enabled) |
| `.paul/SPECIAL-FLOWS.md` | Created (init — frontend-design, simplify, feature-dev) |
| `.paul/phases/01-ui-optimierung/01-01-PLAN.md` | Created |
| `.paul/phases/01-ui-optimierung/01-01-SUMMARY.md` | Created |
| `.paul/phases/01-ui-optimierung/01-02-PLAN.md` | Created |
| `.paul/phases/01-ui-optimierung/01-02-SUMMARY.md` | Created |

---

## Key Decisions (für Resume-Kontext)

- **Tool-btn Mobile-Width 40px** statt 44px — Trade-off mit 360px-Viewports (DONE_WITH_CONCERNS für AC-3 in 01-01)
- **Schwebende Mobile-Trigger ohne Hintergrund** — User-Wunsch; Lesbarkeit auf Papier via dunkler Color-Override (`#2b2840`)
- **Zen-Mode hält Trigger inline** — Override in `body.zen-mode` gegen mobile-MQ position:fixed
- **Manifest-Icons als `purpose: "any maskable"`** — kombiniert für Launcher-Kompatibilität
- **Skill `/frontend-design` required** für UI/Layout-Plans (aus SPECIAL-FLOWS.md) — nächster Plan 01-03 ist UI → Skill MUSS vor APPLY geladen sein

---

## What's Next

**Immediate:** `/paul:plan 01-03` — Zen-Mode-Layout-Fix erstellen (siehe Vorschlag oben).

**After that:**
- `/paul:apply 01-03` (mit `/frontend-design` skill geladen)
- `/paul:unify 01-03`
- Entscheidung: weitere Phase-1-Plans (z.B. Toolbar-Konsolidierung) oder Phase-Transition zu Phase 2 (Sync-Edge-Cases Mobile↔Desktop)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (2 closed, 01-03 queued) |
| `.paul/SPECIAL-FLOWS.md` | Skill-Requirements (frontend-design = required für UI) |
| `.paul/phases/01-ui-optimierung/01-02-SUMMARY.md` | Letzter abgeschlossener Plan |
| `app.html` | UI-Shell — wird in 01-03 wieder modifiziert |
| `CLAUDE.md` | Repo-Konventionen (kein Build-Step, Sync-Architektur, etc.) |

---

## Resume Instructions

1. Lies `.paul/STATE.md` für aktuelle Position
2. Lies dieses Handoff für vollen Session-Kontext
3. Optional: `/paul:resume` oder `/paul:progress`
4. Direkter Weg: `/paul:plan` (greift Phase 1, schlägt 01-03 vor)
5. Beim APPLY von 01-03: `/frontend-design` Skill MUSS vorher geladen sein (sonst BLOCK)

---

*Handoff created: 2026-05-05*
